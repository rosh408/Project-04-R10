import React, {Component} from 'react';
import styles from './styles';

class Schedule extends Component {
  render() {
    return <Text styles={styles.title}>Schedule</Text>;
  }
}

export default Schedule;
